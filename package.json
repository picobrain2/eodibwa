{
  "name": "eodibwa-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "pregenerate": "esbuild scripts/pregenerate-recommendations.ts --bundle --platform=node --format=esm --outfile=scripts/.pregenerate.mjs && node scripts/.pregenerate.mjs",
    "merge-recommendations": "PREGENERATE_MERGE_ONLY=1 npm run pregenerate",
    "build": "tsc --noEmit && vite build",
    "build:all": "npm run pregenerate && npm run build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
